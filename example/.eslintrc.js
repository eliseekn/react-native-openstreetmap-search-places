module.exports = {
    root: true,
    extends: '@react-native-community',
    rules: {
        indent: ['error', 4, {SwitchCase: 1}],
        quotes: ['error', 'single'],
        semi: ['error', 'never'],
    },
}
